---
/**
 * Main entry point.
 * URL: /?t={token}
 * If no token is present, CatalogShell shows a friendly landing message.
 * The React island reads the token client-side and fetches from n8n.
 */

import CatalogLayout from "../layouts/CatalogLayout.astro";
import CatalogShell from "../components/catalog/CatalogShell";
import "../styles/global.css";
---

<CatalogLayout
    title="Catálogo"
    description="Explora nuestros productos y haz tu pedido"
>
    <CatalogShell client:load />
</CatalogLayout>
